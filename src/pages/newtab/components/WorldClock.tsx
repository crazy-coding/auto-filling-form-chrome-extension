const WorldClock = ({ func, ...props }) => {
    const { id, title, widget, position, style, minimized, meta } = props;
    return <div className="WidgetComponent WorldClock">WorldClock</div>
}

export default WorldClock;