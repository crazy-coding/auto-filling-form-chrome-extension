const SuggestedLinks = (props) => {
  const { id, title, widget, position, style, minimized, meta, func } = props;
  return <div className="WidgetComponent SuggestedLinks">SuggestedLinks</div>
}

export default SuggestedLinks;